import React from 'react';

import TaskDeleteButton from './TaskDeleteButton';

export default {
  title: 'Components/TaskDeleteButton',
  component: TaskDeleteButton,
};

export const basic = () => <TaskDeleteButton onClick={() => {}} />;
